SELECT 
  r.contest_id, 
  ROUND(100.00 * COUNT(DISTINCT r.user_id)/ (SELECT COUNT(*) FROM Users),2) AS percentage
FROM Register AS r
JOIN Users AS u
USING(user_id)
GROUP BY r.contest_id
ORDER BY percentage DESC, contest_id
